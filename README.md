# truthimiser
A typo maker!

Run on Linux (and hopefully with the same binary and method on MacOS), simply by
```./truthimiser_linux.out```
Or build your own with gcc from source.
On Windows, run from the icon - or via cmd/powershell by
```.\truthimiser_win.exe```
